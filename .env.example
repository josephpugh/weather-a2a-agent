# Copy to .env and fill in to run the server against a real LLM.
# (Not needed for `--demo` mode or the test suite.)

OPENAI_API_KEY=sk-...
OPENAI_CHAT_MODEL=gpt-4o-mini

# Optional server bind overrides
HOST=0.0.0.0
PORT=9999
